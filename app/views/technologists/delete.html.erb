<% @page_title = "Technologists - Delete" %>

<div>
	<div>
		<h2>Delete Technologist</h2>
		<%= link_to("Back to List", {action: 'index', search: @search_value, sort: @sort_direction}, class: 'btn btn-info') %>
	</div>
	<%= simple_form_for(@technologist,
				 		url: {
						 	action: 'destroy',
						 	id: @technologist.id,
						 	search: @search_value,
						 	sort: @sort_direction
						},
						method: 'delete') do |f| %>
		<p>Are you sure you want to permanently delete this technologist?</p>
		<p><%= @technologist.name %></p>
		<%= f.button :submit, 'Submit', class: 'btn btn-primary' %>
	<% end %>
</div>